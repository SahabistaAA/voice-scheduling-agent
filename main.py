from source.app import app

# This file exists solely to provide an entrypoint for Vercel and Railway's Python runtime.
# Specifically, Railway's Nixpacks builder looks for the exact string: FastAPI(
# app = FastAPI()
